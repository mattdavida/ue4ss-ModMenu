{
  "name": "modmenu",
  "private": true,
  "type": "module",
  "scripts": {
    "bundle": "node tools/bundle.mjs",
    "deploy": "node tools/deploy.mjs",
    "test": "dotnet test tooling/ModMenu.sln --nologo",
    "studio": "dotnet run --project tooling/Studio --no-launch-profile"
  }
}
